# 项目进度

## 2026-08-13 项目初始化与 V1 架构基线

1. 目标：将 `NewProject` 模板独立初始化为 `03桌面` Android 项目，并在业务施工前固化产品、架构、环境、权限和验证边界。
2. 仓库：目标目录原为空；模板无未提交修改；目标 Git 已建立 `main` 分支并配置 `origin`，远端公开仓库有效且大小为 0、无引用。初始化检查时尚未暂存、提交或推送；后续已取得本轮首次本地提交授权。
3. 项目身份：`desktop-client`（Android 9 原生车机客户端），包名 `com.tcrrry.desktop`；`03歌词` 包名 `com.tcrrry.desktoplyrics`，不共享包名、签名、源码或状态。
4. 工程底座：单 `app` 模块、Kotlin/XML Views、AGP 8.2.2、Gradle 8.2、Java 17、`compileSdk=34`、`minSdk/targetSdk=28`，当前只有无权限启动壳。
5. 环境：本机 JDK 17、Android Platform 34、Build Tools 34.0.0、Platform Tools 和首选 ADB 已确认；车机 `S56_HQX` 在线，SDK 28、`1920 x 1080`。
6. 系统能力：只读确认第三方 launcher、显式启动组件、原生卸载器、原生 APK 安装器、未知来源授权、Download 和已挂载公共外部卷入口；未启动、安装、卸载、授权或修改设备。
7. 文档：已补产品基线、项目总纲、V1 物理施工方案、Android 技术栈/身份决策、自身显示/卸载建议、开发环境、安全边界、车机能力记录和验证矩阵。
8. 规则沉淀：Android 分层、固定车机 UI、权限白名单、设备写操作和真机验证规则已写入当前项目分类文档；这些是项目专属事实，模板已有对应槽位，无需回流 `NewProject`。
9. 当时待确认：决策 0002 建议 V1 隐藏自身且不从自身面板发起自卸载；该事项已在后续记录中确认。
10. 未完成：V1 业务代码、业务自动化测试、真机运行 smoke、独立 release keystore、正式版本与部署/分发均未执行。
11. 验证：模板源快检、项目就绪、skills、Android 配置、脚本语法、工具链/车机基线、系统入口只读探测、Kotlin 编译、资源处理、Lint 和 Debug 构建均通过；单测任务为 `NO-SOURCE`，因为尚未施工业务逻辑。
12. 真机启动：Debug APK 已安装到车机，`com.tcrrry.desktop/.MainActivity` 启动成功且进程存在；截图确认横屏界面显示“03桌面”，无崩溃、空白、方向错误或文本越界。未授权、未启动服务，业务运行 smoke 仍因尚未施工而不可执行。
13. 保护状态：收尾时 `03歌词` 工作树存在用户未提交修改，本轮未覆盖、暂存、回退或提交；`NewProject` 工作树无未提交修改，本轮未改动模板源。

## 2026-08-13 架构确认与敏捷协作基线

1. 用户已确认决策 0002：V1 精确隐藏 `com.tcrrry.desktop`，不从自身面板发起自卸载，但保留 Android 设置、原厂应用管理等标准卸载入口。
2. `com.tcrrry.desktoplyrics` 不命中自身规则，必须与其它普通第三方应用一样显示、排序并支持进入 Android 原生卸载确认页。
3. V1 架构方案已获准进入连续施工；默认在同一个施工对话内完成整个 V1，内部仅保留 3 个技术检查点：悬浮抽屉与恢复基础、应用目录与管理主链、APK 安装/快捷控制/整体收口。
4. 项目采用短周期车机闭环：每个内部检查点完成相关自动检查、测试、Debug 构建、自动覆盖安装和最短 smoke；3 个检查点全部完成后再一次性交给用户做更广体验验收，反馈问题后继续修复，确认无问题后再提交。
5. 这套节奏已沉淀到项目 `AGENTS.md`、AI 协作规则、验证规则和验证矩阵；它是当前项目事实，不回流为 `NewProject` 的跨项目默认规则。
6. 本轮只有文档与约束变化，没有修改 Kotlin、资源或 Manifest；复用初始化阶段已经通过的构建、安装和启动壳 smoke，不重复安装无业务变化的 APK。
7. 已按用户授权创建首次本地提交；未推送、未部署、未发布。

## 2026-08-14 V1 连续施工与扩展体验交接

1. 已实现：右侧悬浮抽屉、手势与窗外收起、前台服务、开机恢复、第三方应用目录、四列网格、拖拽排序、系统应用信息页卸载、包变化刷新、APK 扫描与系统安装器、上一首/播放暂停/下一首。
2. 产品边界：移除了返回首页和音量控制；快捷区只保留三枚媒体键。`com.tcrrry.desktop` 只按精确包名隐藏，`com.tcrrry.desktoplyrics` 仍作为普通第三方显示、排序、启动并可进入系统应用信息页。
3. 固件兼容结论：从悬浮服务直接发起 `ACTION_DELETE` 会产生可见但不可点击的确认框，已改为进入目标应用的系统“应用信息”页，再由系统继续原生卸载确认；不申请 `REQUEST_DELETE_PACKAGES`、不使用无障碍、ADB 或车厂私有接口作为产品路径。
4. 真机已完成：悬浮服务与抽屉最短 smoke、应用目录、精确隐藏自身、`03歌词` 普通资格、排序持久化、包变化刷新；用户已完成实际 APK 安装及专门测试包 `com.tcrrry.desktop.smoketest` 的卸载。微信和 `03歌词` 未被卸载。
5. 自动验证已完成：项目就绪、skills、Android 配置、车机只读能力检查、17 项单元测试、Lint、非缓存 Debug 构建和 `git diff --check` 均通过。`check-template.mjs` 因本仓库已初始化为具体项目而按预期拒绝，不构成 V1 失败。
6. 已交给用户扩展体验：窗外点击收起并穿透、普通第三方启动、连续拖拽、真实媒体会话、地图边缘、倒车影像/安全提示和开机恢复。视觉、尺寸、间距、颜色和交互的大改不在本次功能收口内。
7. Git：用户已确认当前体验并明确要求提交；对应改动已纳入本地提交，未推送。Debug 覆盖安装仅用于开发验证，不是发布或上线。

## 2026-08-14 敏捷协作规则纠偏

1. 用户确认本项目改为“最小功能切片 + 基础机器检查 + 用户主手测”，不再采用整版 V1 同一对话连续施工或 AI 主验收。
2. 普通 Kotlin、资源和 Manifest 切片默认只执行 `assembleDebug`，命中已有直接相关单测时追加该测试；完整单测、Lint 和真机 smoke 只在里程碑、提交/发布准备、高风险变更或用户明确要求时执行。
3. 普通功能请求直接施工，不强制经历分析确认、架构方案和再次下达“开始施工”；安全红线、破坏性设备操作、正式发布和显著范围歧义仍保留人工门禁。
4. 用户可以在任一切片后开始手测并反馈；凡产生可手测行为变化，AI 在基础检查通过后自动把当前 Debug 包覆盖安装到目标车机并确认进程与悬浮服务。安装其它 APK、卸载、授权、清数据、重启和改设置继续单独确认。
5. `task-closeout` 只用于明确要求的提交、交接、handoff、归档、发布准备或正式收尾；普通交付不再自动更新多份文档、重跑验证或汇报无关状态。
6. 2026-08-13 的“连续施工与三个内部检查点”记录仅保留为历史事实，已被本节和当前根 `AGENTS.md` 覆盖；V1 方案继续作为架构、权限和系统适配边界，不再规定交付节奏。
7. 该协作偏好是 `03桌面` 项目事实，不回流 `NewProject`；本轮未修改模板源。

## 2026-08-14 抽屉视觉与媒体范围纠偏

1. 用户反馈旧抽屉把“安装 APK”放在页首、应用网格贴顶，并让固定媒体区与拖拽卸载区占用同一层级，导致产品身份混乱和功能区域重叠。
2. 抽屉已改为“顶部应用标题与留白 -> 主体四列网格 -> 底部次级安装动作”；拖拽时底部同一槽位互斥切换为卸载目标，安装与卸载不再同时出现。
3. 视觉资源采用 `03lyrics/docs/architecture/iCAR车机UI设计规范.md` 中已从原车应用中心取证的页面模式 B 数值与语义：保留 `42 / 84 / 18 px` 页面节奏与 `108 px` 全屏图标基准；针对右侧小窗口将运行图标收敛为 `96 px`、标签设为 `24 sp`、行步长设为 `194 px`，并以 `38 sp` 加粗标题标识“第三方应用中心”。没有复制 `03歌词` 源码、包名、签名或原车私有资产。
4. 目标固件上的三个全局媒体键不能可靠控制当前媒体会话；补齐封面、歌曲、播放状态和进度需要通知使用权，而该权限不在 V1 白名单中。因此已移除媒体 UI、控制器和调试动作，未来只有专项权限与 MediaSession 方案获确认后才重新评估。
5. 本轮规则属于 `03桌面` 固定车机布局和权限边界，已沉淀到设计与产品规则，不是所有新项目通用默认能力，不回流 `NewProject`。

## 2026-08-14 标准浮窗与桌面表面占用联动

1. 已沿应用启动主链增加通用标准浮窗协调：只依据目标 Activity metadata 判定，`window_mode=0` 直接启动，`2` 时注册一次性观察并先退 HOME，观察到 `0` 后只启动目标一次；未知、读取失败、HOME/目标失败和 `3000 ms` 超时均清理请求并明确失败。
2. 已沿抽屉唯一状态机增加桌面表面占用语义，并由 `OverlayService` 持有通用绑定租约客户端；有效展开前取得、展开和收起期间保持、完全 PARKED 后释放，配置变化和快速反向不主动闪断。
3. Manifest 只增加通用 Service Action 的 `<queries>`，未新增系统权限；主源码没有歌词包名、Secure 写入、任务枚举、force-stop、固定 sleep、周期轮询或 Binder 业务调用。
4. 新增 16 条直接相关协调器测试并通过；项目就绪、Android 配置、`git diff --check` 和 Debug 构建通过，Debug APK 已覆盖安装到 `S56_HQX`，进程与悬浮服务正常。
5. 当前车机未发现协议版本 1 的占用租约服务；已验证无提供方时抽屉仍可展开和完全收回。用户已确认标准浮窗返回栈与反向系统窗口路径正常；歌词大区域随租约退出/恢复仍待对应提供方版本上车后验证。
6. 本轮未修改 `03歌词` 仓库；对应实现纳入本次本地提交，不推送。

## 记录规则

1. 只记录已发生事实、验证结果、未完成事项和下一步。
2. 不复制长日志，不把方案或待确认决策写成已实现事实。
