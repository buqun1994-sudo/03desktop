# 项目进度

## 2026-08-13 项目初始化与 V1 架构基线

1. 目标：将 `NewProject` 模板独立初始化为 `03桌面` Android 项目，并在业务施工前固化产品、架构、环境、权限和验证边界。
2. 仓库：目标目录原为空；模板无未提交修改；目标 Git 已建立 `main` 分支并配置 `origin`，远端公开仓库有效且大小为 0、无引用。初始化检查时尚未暂存、提交或推送；后续已取得本轮首次本地提交授权。
3. 项目身份：`desktop-client`（Android 9 原生车机客户端），包名 `com.tcrrry.desktop`；`03歌词` 包名 `com.tcrrry.desktoplyrics`，不共享包名、签名、源码或状态。
4. 工程底座：单 `app` 模块、Kotlin/XML Views、AGP 8.2.2、Gradle 8.2、Java 17、`compileSdk=34`、`minSdk/targetSdk=28`，当前只有无权限启动壳。
5. 环境：本机 JDK 17、Android Platform 34、Build Tools 34.0.0、Platform Tools 和首选 ADB 已确认；车机 `S56_HQX` 在线，SDK 28、`1920 x 1080`。
6. 系统能力：只读确认第三方 launcher、显式启动组件、原生卸载器、原生 APK 安装器、未知来源授权、Download 和已挂载公共外部卷入口；未启动、安装、卸载、授权或修改设备。
7. 文档：已补产品基线、项目总纲、V1 物理施工方案、Android 技术栈/身份决策、自身显示/卸载建议、开发环境、安全边界、车机能力记录和验证矩阵。
8. 规则沉淀：Android 分层、固定车机 UI、权限白名单、设备写操作和真机验证规则已写入当前项目分类文档；这些是项目专属事实，模板已有对应槽位，无需回流 `NewProject`。
9. 当时待确认：决策 0002 建议 V1 隐藏自身且不从自身面板发起自卸载；该事项已在后续记录中确认。
10. 未完成：V1 业务代码、业务自动化测试、真机运行 smoke、独立 release keystore、正式版本与部署/分发均未执行。
11. 验证：模板源快检、项目就绪、skills、Android 配置、脚本语法、工具链/车机基线、系统入口只读探测、Kotlin 编译、资源处理、Lint 和 Debug 构建均通过；单测任务为 `NO-SOURCE`，因为尚未施工业务逻辑。
12. 真机启动：Debug APK 已安装到车机，`com.tcrrry.desktop/.MainActivity` 启动成功且进程存在；截图确认横屏界面显示“03桌面”，无崩溃、空白、方向错误或文本越界。未授权、未启动服务，业务运行 smoke 仍因尚未施工而不可执行。
13. 保护状态：收尾时 `03歌词` 工作树存在用户未提交修改，本轮未覆盖、暂存、回退或提交；`NewProject` 工作树无未提交修改，本轮未改动模板源。

## 2026-08-13 架构确认与敏捷协作基线

1. 用户已确认决策 0002：V1 精确隐藏 `com.tcrrry.desktop`，不从自身面板发起自卸载，但保留 Android 设置、原厂应用管理等标准卸载入口。
2. `com.tcrrry.desktoplyrics` 不命中自身规则，必须与其它普通第三方应用一样显示、排序并支持进入 Android 原生卸载确认页。
3. V1 架构方案已获准进入连续施工；默认在同一个施工对话内完成整个 V1，内部仅保留 3 个技术检查点：悬浮抽屉与恢复基础、应用目录与管理主链、APK 安装/快捷控制/整体收口。
4. 项目采用短周期车机闭环：每个内部检查点完成相关自动检查、测试、Debug 构建、自动覆盖安装和最短 smoke；3 个检查点全部完成后再一次性交给用户做更广体验验收，反馈问题后继续修复，确认无问题后再提交。
5. 这套节奏已沉淀到项目 `AGENTS.md`、AI 协作规则、验证规则和验证矩阵；它是当前项目事实，不回流为 `NewProject` 的跨项目默认规则。
6. 本轮只有文档与约束变化，没有修改 Kotlin、资源或 Manifest；复用初始化阶段已经通过的构建、安装和启动壳 smoke，不重复安装无业务变化的 APK。
7. 已按用户授权创建首次本地提交；未推送、未部署、未发布。

## 2026-08-14 V1 连续施工与扩展体验交接

1. 已实现：右侧悬浮抽屉、手势与窗外收起、前台服务、开机恢复、第三方应用目录、四列网格、拖拽排序、系统应用信息页卸载、包变化刷新、APK 扫描与系统安装器、上一首/播放暂停/下一首。
2. 产品边界：移除了返回首页和音量控制；快捷区只保留三枚媒体键。`com.tcrrry.desktop` 只按精确包名隐藏，`com.tcrrry.desktoplyrics` 仍作为普通第三方显示、排序、启动并可进入系统应用信息页。
3. 固件兼容结论：从悬浮服务直接发起 `ACTION_DELETE` 会产生可见但不可点击的确认框，已改为进入目标应用的系统“应用信息”页，再由系统继续原生卸载确认；不申请 `REQUEST_DELETE_PACKAGES`、不使用无障碍、ADB 或车厂私有接口作为产品路径。
4. 真机已完成：悬浮服务与抽屉最短 smoke、应用目录、精确隐藏自身、`03歌词` 普通资格、排序持久化、包变化刷新；用户已完成实际 APK 安装及专门测试包 `com.tcrrry.desktop.smoketest` 的卸载。微信和 `03歌词` 未被卸载。
5. 自动验证已完成：项目就绪、skills、Android 配置、车机只读能力检查、17 项单元测试、Lint、非缓存 Debug 构建和 `git diff --check` 均通过。`check-template.mjs` 因本仓库已初始化为具体项目而按预期拒绝，不构成 V1 失败。
6. 已交给用户扩展体验：窗外点击收起并穿透、普通第三方启动、连续拖拽、真实媒体会话、地图边缘、倒车影像/安全提示和开机恢复。视觉、尺寸、间距、颜色和交互的大改不在本次功能收口内。
7. Git：用户已确认当前体验并明确要求提交；对应改动已纳入本地提交，未推送。Debug 覆盖安装仅用于开发验证，不是发布或上线。

## 2026-08-14 敏捷协作规则纠偏

1. 用户确认本项目改为“最小功能切片 + 基础机器检查 + 用户主手测”，不再采用整版 V1 同一对话连续施工或 AI 主验收。
2. 普通 Kotlin、资源和 Manifest 切片默认只执行 `assembleDebug`，命中已有直接相关单测时追加该测试；完整单测、Lint 和真机 smoke 只在里程碑、提交/发布准备、高风险变更或用户明确要求时执行。
3. 普通功能请求直接施工，不强制经历分析确认、架构方案和再次下达“开始施工”；安全红线、破坏性设备操作、正式发布和显著范围歧义仍保留人工门禁。
4. 用户可以在任一切片后开始手测并反馈；凡产生可手测行为变化，AI 在基础检查通过后自动把当前 Debug 包覆盖安装到目标车机并确认进程与悬浮服务。安装其它 APK、卸载、授权、清数据、重启和改设置继续单独确认。
5. `task-closeout` 只用于明确要求的提交、交接、handoff、归档、发布准备或正式收尾；普通交付不再自动更新多份文档、重跑验证或汇报无关状态。
6. 2026-08-13 的“连续施工与三个内部检查点”记录仅保留为历史事实，已被本节和当前根 `AGENTS.md` 覆盖；V1 方案继续作为架构、权限和系统适配边界，不再规定交付节奏。
7. 该协作偏好是 `03桌面` 项目事实，不回流 `NewProject`；本轮未修改模板源。

## 2026-08-14 抽屉视觉与媒体范围纠偏

1. 用户反馈旧抽屉把“安装 APK”放在页首、应用网格贴顶，并让固定媒体区与拖拽卸载区占用同一层级，导致产品身份混乱和功能区域重叠。
2. 抽屉已改为“顶部应用标题与留白 -> 主体四列网格 -> 底部次级安装动作”；拖拽时底部同一槽位互斥切换为卸载目标，安装与卸载不再同时出现。
3. 视觉资源采用 `03lyrics/docs/architecture/iCAR车机UI设计规范.md` 中已从原车应用中心取证的页面模式 B 数值与语义：保留 `42 / 84 / 18 px` 页面节奏与 `108 px` 全屏图标基准；针对右侧小窗口将运行图标收敛为 `96 px`、标签设为 `24 sp`、行步长设为 `194 px`，并以 `38 sp` 加粗标题标识“应用管理”。没有复制 `03歌词` 源码、包名、签名或原车私有资产。
4. 目标固件上的三个全局媒体键不能可靠控制当前媒体会话；补齐封面、歌曲、播放状态和进度需要通知使用权，而该权限不在 V1 白名单中。因此已移除媒体 UI、控制器和调试动作，未来只有专项权限与 MediaSession 方案获确认后才重新评估。
5. 本轮规则属于 `03桌面` 固定车机布局和权限边界，已沉淀到设计与产品规则，不是所有新项目通用默认能力，不回流 `NewProject`。

## 2026-08-14 标准浮窗与桌面表面占用联动

1. 已沿应用启动主链增加通用标准浮窗协调：只依据目标 Activity metadata 判定；实机确认 `window_mode=0/1/2/3` 分别为无占用、仅左侧 ADAS 卡片、仅标准浮窗、两者并存，`0/1` 直接启动，`2/3` 注册一次性观察并先退 HOME，观察到 `0/1` 后只启动目标一次；其它值、读取失败、HOME/目标失败和 `3000 ms` 超时均清理请求并明确失败。
2. 已沿抽屉唯一状态机增加桌面表面占用语义，并由 `OverlayService` 持有通用绑定租约客户端；有效展开前取得、展开和收起期间保持、完全 PARKED 后释放，配置变化和快速反向不主动闪断。
3. Manifest 只增加通用 Service Action 的 `<queries>`，未新增系统权限；主源码没有歌词包名、Secure 写入、任务枚举、force-stop、固定 sleep、周期轮询或 Binder 业务调用。
4. 标准浮窗协调器的 16 条直接相关测试已通过；项目就绪、Android 配置、`git diff --check` 和 Debug 构建通过，Debug APK 已覆盖安装到 `S56_HQX`，进程与悬浮服务正常。
5. 当前车机已发现 `03歌词` 提供的协议版本 1 占用租约服务；无提供方时抽屉仍保持原行为。用户已通过充电强制 ADAS 场景主测：右侧抽屉展开/收起可驱动歌词顶栏切换，`window_mode=1` 时可直接进入 APK 安装页，不再误报浮窗切换失败。
6. 本轮未修改 `03歌词` 仓库；对应实现纳入本次本地提交，不推送。

## 2026-08-15 Fossify 文件管理器车机安装基线（历史）

1. 用户已在 `S56_HQX` 上确认 Fossify File Manager 车机放大版可用；应用内部固定按原界面的 `175%` 缩放，不提供缩放设置，启动器名称保持原版“文件管理器”。
2. 原官方包 `org.fossify.filemanager` 已按用户授权卸载；当前车机安装包为 `org.fossify.filemanager.debug`，版本名 `1.6.1-car175`，存储读写权限与未知来源 APK 安装能力已恢复。
3. 当日车机安装、覆盖更新和恢复基线为 `Fossify-File-Manager-1.6.1-Car-175.apk`，SHA-256 为 `6b029b6a7d95ccb43b47c024d79ed337ec9c94524847bbed8765eb4296ac0db1`；该基线已被 2026-08-17 的 `1.6.1-car175.1` 增强版替代。
4. 该基准 APK 使用当前 Android Debug 证书签名；后续若重新构建替换包，必须保持同一包名和签名才能无清数据覆盖安装，否则应继续直接使用本条记录的基准 APK。
5. 官方 `1.6.1` 本地备份已从下载目录移除，当前下载目录只保留上述车机放大版安装包；若用户明确要求回退官方版，应从 Fossify 官方发布页重新获取并重新确认卸载数据影响。

## 2026-08-15 启动授权与文件管理器入口收敛（历史口径）

1. `03桌面` 启动入口已改为无界面地尝试启动前台服务并立即结束；删除自动检查后弹出悬浮授权页的逻辑、授权说明界面和相关文案。服务与开机恢复仍保留必要的权限防守检查，但不会主动打开授权界面。
2. 精确包名 `org.fossify.filemanager.debug` 已加入应用管理策略：它继续在抽屉中显示、排序和启动，但从 `03桌面` 拖拽时不展示或进入卸载目标；`03歌词` 与其它普通第三方应用的卸载资格不变。
3. 底部“安装 APK”已改为通过统一标准浮窗互斥链打开该文件管理器；包缺失、被禁用或入口失效时显示明确提示。旧内置 APK 扫描 Activity、扫描器、SAF 授权存储、FileProvider、布局资源和测试均已删除。
4. `03桌面` Manifest 已移除 `READ_EXTERNAL_STORAGE` 与 `REQUEST_INSTALL_PACKAGES`，当前只保留悬浮窗、前台服务和开机恢复三项权限；文件浏览、存储授权和系统安装器跳转由独立文件管理器负责。
5. Android 配置、项目就绪、差异格式、固定文件管理器保护、保护项拖拽、标准浮窗互斥共 `23` 条直接相关测试及 Debug 构建均通过。最新 Debug APK 已覆盖安装到 `S56_HQX`，进程与悬浮服务正常；固定文件管理器包与 launcher 入口已在车机解析成功。
6. 现成文件管理器 APK 即使作为资源放进 `03桌面` APK，安装后仍必须成为第二个 Android 包，不能获得单应用身份。真正合成一个应用需要基于文件管理器源码迁移 Activity、资源、依赖和权限，并专项处理上游许可、Compose/构建栈、目标 SDK、体积与安全边界；本轮未执行该重构。
7. “安装 APK”入口现已默认隐藏，只有精确包名 `org.fossify.filemanager.debug` 当前已安装、启用且 launcher 可解析时才显示，不限制版本；安装、卸载、禁用和更新事件沿现有合并刷新自动更新按钮状态，展示与点击共用同一目标解析逻辑。
8. 本节记录的是当日实现；“安装 APK 依赖文件管理器”和“文件管理器不提供卸载目标”两项口径已被 2026-08-17 的产品重构明确替代。

## 2026-08-15 全局返回正式化与项目清理

1. 用户确认右侧触发条轻点执行系统返回是正式产品能力，不再作为 Debug 测试功能；向左滑动仍只负责展开抽屉。
2. AccessibilityService 的实现、声明、说明文案和最小能力 XML 已从 Debug 专属源集迁入 `main`。为保持覆盖安装后的既有无障碍授权，运行组件名继续使用 `com.tcrrry.desktop.debug.NavigationDemoAccessibilityService`；历史命名只作为兼容锚点，不代表调试能力。
3. 服务只调用公开 `GLOBAL_ACTION_BACK`；已删除三键导航实验界面、HOME/RECENTS 动作和无障碍悬浮层代码。打包后的三项能力 `canRetrieveWindowContent`、`canRequestFilterKeyEvents`、`canPerformGestures` 均为 `false`，事件回调不读取内容。
4. 产品基线、长期总纲、V1 方案、安全边界、代码/产品/测试规则和验证矩阵已同步；Android 配置检查新增正式组件、单一返回动作和三项隐私配置护栏。
5. 已删除模板时代且不适用于 Android 资源主链的 `check-localization.mjs`、Debug 专属无障碍资源、禁用的三键导航代码，以及 6 个确定无引用的样式/颜色/尺寸/背景资源。
6. 小规模自有网页侧载仍决定使用独立 Release 证书，避免开发机 Debug 证书变化后用户无法覆盖升级；本轮未生成密钥、未接入 signingConfig、未构建或分发 Release，后续在用户确认密钥保存与双份备份位置后单独执行。
7. 无缓存完整执行 `testDebugUnitTest lintDebug assembleDebug`：41 项单元测试全部通过，Lint 为 0 错误、22 条非阻断警告且无未使用资源警告；项目就绪、skills、Android 配置、脚本语法和 `git diff --check` 均通过。
8. 用户已通过手机 ADB助手完成三款应用重新安装、授权和 03桌面启动，并反馈本轮链路测试成功。电脑端 ADB 当前离线，未安装本次新 APK；下一次用户手动覆盖后只需复验关闭态轻点返回和有效左滑展开。
9. 本轮未暂存、提交或推送任何改动，后续支付需求也未进入本次施工范围。

## 2026-08-17 中等加粗 Logo 矢量化与运行资源收敛

1. 用户确认采用中等加粗的蓝红 `03 / DESKTOP` 定稿；新增 `03desktop-logo.svg` 作为唯一矢量母版，数字、连接点和英文字标均为独立路径，不嵌入位图、不依赖本机字体。
2. 新增根目录 `03desktop-logo.png` 作为由母版导出的 `1024 x 1024` RGB 预览图；Android 清单继续引用 `@drawable/ic_launcher_logo`，运行资源改为由同一母版直接导出的 `216 x 216` RGB PNG，供原车应用中心按 `108 x 108px` 显示。
3. SVG 结构、禁用位图 / 文本节点、PNG 尺寸与色彩模式、清单引用和 `108px` 实际显示尺寸均已静态核对；按用户要求，本轮未重新打包、未覆盖安装车机，后续随其它功能覆盖安装时观察实机效果。

## 2026-08-17 APK 安装与 U 盘增强边界重构

1. 用户重新确认产品主身份是第三方应用启动与管理；文件管理器调整为官网下载的可选增强软件，不再承担或控制 `03桌面` 的“安装 APK”主入口。
2. `03桌面` 已恢复内置 Download APK 页面、可取消扫描、FileProvider 和系统安装器主链；Download 与未知来源安装资格由手机安装工具统一前置授予，页面不再承载授权流程。抽屉底部“安装 APK”始终可用，与文件管理器安装状态解耦。
3. 安装页只在精确文件管理器 Activity 声明 `CAR_INTEGRATION_VERSION>=1` 时显示“打开 U 盘”；点击走统一浮窗互斥链和显式动作，成功启动后关闭旧安装页。文件管理器缺失、禁用或版本不兼容时只隐藏该按钮。
4. 增强文件管理器升级为 versionCode `14`、versionName `1.6.1-car175.1`，保留固定 `175%` 车机缩放，移除全部系统 Launcher 入口，并通过公开 `StorageManager` 解析可移动卷。`03桌面` 显式把其 `MainActivity` 合成到应用网格，且文件管理器恢复普通卸载资格。
5. 通过仓库可复现脚本重新生成的增强 APK `Fossify-File-Manager-1.6.1-Car-175.1.apk` 的 SHA-256 为 `8f64699f6fb2bb4b5006b57e20e994b6946518eb749fa07fa4ff6a7c1726c1b4`，签名证书 SHA-256 为 `2990047fddf6d6ec1eb7f83731fcc1398616e5fb83aec97542a4f132c35a1a27`；已用同一 Debug 证书无清数据覆盖安装。该包仅为开发验证，尚未建立或分发正式 Release。
6. Fossify 上游 `1.6.1` / commit `6879b7871a10057df197b73508835c8772d98e47`、GPLv3 许可证、完整改造补丁和可复现构建脚本已固化到 `vendor/fossify-file-manager-car/`；未来官网分发二进制时必须同时提供对应源码入口。
7. 车机已验证增强版没有 `MAIN + LAUNCHER` 查询结果，但公开 `MainActivity` 可显式解析；U 盘动作实际进入标题“USB”的 `/storage/udisk` 根目录并显示真实内容。
8. 用户指出旧页授权与手机安装授权流程重复。当前口径改为安装阶段统一授予 `READ_EXTERNAL_STORAGE` 和 `REQUEST_INSTALL_PACKAGES` AppOp：Debug 覆盖脚本使用 `adb install -r -g` 并核验 AppOp，安装页删除“允许读取”、`requestPermissions` 和未知来源设置跳转；任一资格意外缺失时只显示安装授权未完成，不在产品页面建立第二套授权流程。
9. 连续验证发现目标固件不会重新唤醒已停止的文件管理器自由窗口任务，普通显式启动只报告任务置前但画面不出现；实机证明 `NEW_TASK | CLEAR_TASK` 可稳定创建新窗口并再次直达 USB。该策略已收进精确文件管理器契约，网格入口和 U 盘动作共用，只清 Activity 任务，不清数据或权限。
10. 最终 Debug 覆盖安装已核验 `READ_EXTERNAL_STORAGE: granted=true`、`REQUEST_INSTALL_PACKAGES: allow`、应用进程和悬浮服务正常。抽屉始终展示“安装 APK”和显式文件管理器；安装页没有授权按钮或授权跳转，直接列出 Download 中 1 个测试 APK；点选后 `com.android.packageinstaller/.PackageInstallerActivity` 实际进入前台，随后返回取消，未安装测试包。
11. 文件管理器系统 Launcher 查询仍为 `No activities found`，公开 `MainActivity` 可显式解析；从 `03桌面` 网格可打开内部存储，从安装页连续两轮点击“打开 U 盘”均重新创建可见窗口并进入 `/storage/udisk` 的“USB”根目录。跳转成功后旧安装页任务已移除，`03桌面` 进程和悬浮服务继续运行。

## 记录规则

1. 只记录已发生事实、验证结果、未完成事项和下一步。
2. 不复制长日志，不把方案或待确认决策写成已实现事实。
